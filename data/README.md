# Data

Place input files in `data/raw/` and generated outputs in `data/processed/`.

Expected raw file for this project:

- `nyflights.csv`

The dataset is not committed to the repository. Keep large or licensed datasets outside Git and document their source here before publishing them.
